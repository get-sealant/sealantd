{
  "name": "sealantd-ts",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@bufbuild/buf": "^1.71.0",
    "@bufbuild/protoc-gen-es": "^2.12.0",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@bufbuild/buf"
    ]
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
